%p#notice= notice

%p
  %b
    Name:
  = @course.name

%p
  %b
    Title:
  = @course.title

%p
  %b
    Section:
  = @course.section

%p
  %b
    Term:
  - if Date.new(Date.today.year, 8, 1) == @course.term
    Fall
  - else
    Spring

- if is_admin? || @course.teacher_id == current_user.id
  .btn= link_to 'Edit', edit_course_path(@course)
  %br
  %br
  .btn= link_to 'Back', courses_path